# DriveMind AI Web

DriveMind AI 前端应用，基于 Vue 3、Vite、Naive UI、Pinia 和 Vue Router 构建。

## 启动

进入前端目录：

```bash
cd web
```

安装依赖：

```bash
npm install
```

启动开发服务：

```bash
npm run dev
```

## 说明

前端默认通过 `VITE_BASE_API=/api/v1` 访问后端接口。本地开发时请先启动后端服务，并确保代理配置指向后端端口。
